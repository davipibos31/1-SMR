"use strict";(function wrap(Unicode,go){Unicode.Loading=go.Class({__construct:function __construct(list,firstNode,lastBlock){this.list=list;this.nodes={};if (lastBlock){this.nodes[lastBlock.num]=firstNode;this.list.blocks.debug.createLoading(lastBlock.num);}this.sample=firstNode;},__destruct:function __destruct(){},create:function create(num){if (!this.nodes[num]){this.nodes[num]=this.sample.clone();this.list.blocks.debug.createLoading(num);this.log("create " + num);}return this.nodes[num];},remove:function remove(num){if (this.nodes[num]){this.nodes[num].remove();delete this.nodes[num];this.log("remove " + num);}},move:function move(from,to){if (!this.nodes[from]){return undefined;}this.nodes[to]=this.nodes[from];delete this.nodes[from];this.list.blocks.debug.createLoading(to);this.log("move " + from + " -> " + to);return this.nodes[to];},log:function log(){}});})(Unicode,go,jQuery);